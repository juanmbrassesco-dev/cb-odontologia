.DS_Store
.env

# Supabase — archivos internos del CLI y claves de pruebas locales
supabase/.temp/
supabase/functions/.env

# Python
__pycache__/

# Salida intermedia del generador del logo: los entregables van en brand/logo/
tools/letras-cb/salida/

# Salida intermedia de tools/armar-artefacto.py: la maqueta con las fotos
# embebidas pesa medio mega y se regenera con un comando. El que se publica
# es ése, no uno versionado.
brand/tableros/15-pagina/artefacto.html

# Copias de un solo uso que se abren para mirar la maqueta sin que Chrome la
# saque del caché. Se generan con un nombre distinto cada vez y se borran; no
# tienen por qué viajar al repo. (Una se coló el 13-sep-2026.)
brand/tableros/15-pagina/ver-*.html

# Capturas para mirar, de un solo uso. No son entregables: se rehacen
# con un comando y pesan. (Puestas el 14-sep-2026.)
revisiones/
